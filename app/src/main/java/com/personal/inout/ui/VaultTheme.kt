package com.personal.inout.ui

import androidx.compose.ui.graphics.Color

// Amber Ochre Palette (Warm, Earthy, Muted)
val AmberBg = Color(0xFF161412)          // Deep warm asphalt background
val AmberSurface = Color(0xFF221F1B)     // Muted clay card surface
val AmberSurfaceAlt = Color(0xFF2E2A25)  // Secondary element container
val AmberAccent = Color(0xFFDEAC64)      // Soft Tuscan ochre / pale gold
val AmberAccentDim = Color(0xFF9E7C48)   // Muted ochre border
val MildSageGreen = Color(0xFF81C784)    // Subtle, eye-friendly green for 'In'
val MildRoseRed = Color(0xFFE57373)      // Subtle, eye-friendly coral-rose for 'Out'
val AmberTextMuted = Color(0xFFA69E94)   // Pale khaki-grey body text
val AmberTextBright = Color(0xFFF3EFEB)  // Soft warm white header text
